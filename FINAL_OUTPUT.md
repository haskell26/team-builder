# Final Output

## 이번 라운드에서 구현한 내용

- 역할 선호도 모델을 `1순위 / 2순위 / 3순위` 에서 `탱커 / 딜러 / 힐러 총 6점 분배` 방식으로 교체했습니다.
- 새로 붙여넣은 플레이어는 기본 `2 / 2 / 2` 로 시작하고, 저장된 예전 `preferenceOrder` 데이터는 `3 / 2 / 1` 점수 분배로 마이그레이션해 다시 사용할 수 있게 했습니다.
- 현재 매치 10명 카드에서 각 역할 점수를 스텝 버튼으로 직접 조정할 수 있게 했고, 한 역할을 바꾸면 나머지 두 역할이 자동 조정되어 합계 6이 항상 유지되도록 만들었습니다.
- 역할 선호도는 후보 정렬 키에 넣지 않았습니다.
  - 후보 정렬은 여전히 `총점 차이 -> 역할군별 차이 합 -> 탱커 차이 -> 언랭 분배 차이 -> 완전 동률 버킷 랜덤` 순서를 유지합니다.
  - 대신 완전히 같은 밸런스 우선순위 안에서는 역할 선호 점수가 더 잘 맞는 조합이 앞쪽에 나올 수 있도록 soft influence 를 넣었습니다.
  - 후보 카드와 최종 편집 슬롯에는 배정 역할 기준 선호 점수 배지를 붙여 즉시 비교할 수 있게 했습니다.
- 이미 후보를 계산한 뒤 선호 점수를 바꾸면 후보 6개와 최종 편집 영역을 즉시 다시 계산하도록 수정했습니다.
- 저장된 플레이어 목록 UI 를 이름 중심의 단순 리스트로 정리했습니다.
  - 정확히 10명 선택 시에만 `선택한 10명 불러오기` 가 활성화됩니다.
  - 개별 `삭제` 버튼과 `전체 삭제` 버튼을 추가했습니다.
  - 삭제 후 선택 수와 버튼 활성화 상태가 즉시 다시 계산됩니다.
- 저장된 10명을 불러오면 입력 textarea 를 canonical TSV 로 다시 채우고, 현재 매치와 선호 점수를 복원하며, 이전 후보/편집 결과는 비운 상태로 되돌리도록 유지했습니다.
- 기존 핵심 기능도 유지했습니다.
  - 헤더 포함/미포함 입력 지원
  - 후보 6개 compact preview
  - `1탱 2딜 2힐` 고정 슬롯 구조
  - 후보 선택 후 최종 편집 로드
  - 슬롯 두 번 클릭 스왑
  - 메인 UI 의 숫자 밸런스 점수 비노출

## 사용 방법

1. 표를 붙여넣거나 저장된 플레이어 10명을 불러옵니다.
2. 현재 매치 영역에서 각 플레이어의 `탱커 / 딜러 / 힐러` 점수를 조정합니다.
3. `팀 나누기` 를 눌러 후보 6개를 비교합니다.
4. 후보 카드를 눌러 최종 편집 영역으로 가져옵니다.
5. 슬롯 두 개를 차례대로 클릭해 즉시 스왑합니다.

## 실행 및 검증

- 실행: `npm test`
  - 결과: 통과
- 실행: `npm run build`
  - 결과: 통과, `dist/` 정적 번들 생성 확인
- 참고: `npm run test:browser`
  - 이번 라운드에서는 실행하지 않았습니다. README 에 Chromium 환경용 실행 방법을 남겼고, 이번 스펙에서는 이 공백을 blocker 로 간주하지 않습니다.

## 남은 제한

- 저장 기능은 현재 브라우저의 로컬 저장소에만 보관되며, 브라우저 간 동기화나 내보내기/가져오기는 없습니다.
- 역할 선호도는 soft signal 로만 사용합니다. 티어 밸런스 우선순위는 유지하고, 같은 밸런스 버킷 안에서만 노출 후보 선택에 영향을 줍니다.
- 실제 브라우저 엔진 기준의 desktop/mobile 스모크는 Chromium 계열 브라우저가 있는 환경에서 후속 실행이 필요합니다.
