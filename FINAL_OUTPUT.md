# Final Output

## 이번 라운드에서 구현한 내용

- 저장된 플레이어 불러오기 영역을 메인 입력 영역 아래의 긴 섹션이 아니라, 입력 영역 오른쪽 사이드바로 분리했습니다.
- 저장된 플레이어 보관함에 `목록 펼치기 / 목록 접기` 토글을 추가했고, 첫 렌더 기본값은 접힘입니다.
- 접힌 상태에서도 `저장 n명`, `선택 n / 10`, `선택한 10명 불러오기`, `전체 삭제` 상태를 바로 확인할 수 있게 유지했습니다.
- 저장된 플레이어 목록을 이름 중심의 한 줄 리스트로 다시 구성했습니다.
- 각 줄은 `선택 상태 + 이름 + 역할 티어 요약 + 선호 6점 요약` 순서로 압축해 보여 주며, 줄 본문 전체가 클릭 hit area 입니다.
- 사이드바가 좁은 폭에서 아래로 쌓여도 저장된 플레이어 행은 세로 카드로 무너지지 않고 한 줄 압축 레이아웃을 유지합니다.
- `삭제` 버튼은 행 선택 토글과 분리되어 있어 개별 삭제와 전체 삭제를 눌러도 의도하지 않은 선택 변경이 생기지 않습니다.
- 목록은 최대 높이 스크롤 컨테이너를 사용하므로 저장된 플레이어가 많아져도 페이지 전체 높이가 과하게 늘어나지 않습니다.
- exact-10 불러오기 흐름은 유지했습니다. 정확히 10명 선택 시에만 `선택한 10명 불러오기` 가 활성화되고, 불러오면 textarea 를 canonical TSV 로 다시 채우며 이전 후보/편집 결과는 비웁니다.
- 기존 역할 선호도 및 후보 계산 흐름도 유지했습니다.
- 새로 붙여넣은 플레이어는 기본 `2 / 2 / 2` 로 시작하고, 저장된 예전 `preferenceOrder` 데이터는 `3 / 2 / 1` 점수 분배로 마이그레이션합니다.
- 현재 매치 10명 카드에서 각 역할 점수를 스텝 버튼으로 직접 조정할 수 있고, 한 역할을 바꾸면 나머지 두 역할이 자동 조정되어 합계 6이 항상 유지됩니다.
- 후보 정렬은 여전히 `총점 차이 -> 역할군별 차이 합 -> 탱커 차이 -> 언랭 분배 차이 -> 완전 동률 버킷 랜덤` 순서를 유지합니다.
- 기존 핵심 기능도 유지했습니다: 헤더 포함/미포함 입력, 후보 6개 compact preview, `1탱 2딜 2힐` 고정 슬롯 구조, 후보 선택 후 최종 편집 로드, 슬롯 두 번 클릭 스왑, 메인 UI 의 숫자 밸런스 점수 비노출.

## 사용 방법

1. 표를 붙여넣거나, 오른쪽 `저장된 플레이어 불러오기` 보관함을 펼쳐 저장된 플레이어 10명을 선택해 불러옵니다.
2. 현재 매치 영역에서 각 플레이어의 `탱커 / 딜러 / 힐러` 점수를 조정합니다.
3. `팀 나누기` 를 눌러 후보 6개를 비교합니다.
4. 후보 카드를 눌러 최종 편집 영역으로 가져옵니다.
5. 슬롯 두 개를 차례대로 클릭해 즉시 스왑합니다.

## 실행 및 검증

- 실행: `npm test`
  - 결과: 통과
- 실행: `npm run build`
  - 결과: 통과, `dist/` 정적 번들 생성 확인
- 참고: `npm run test:browser`
  - 이번 라운드에서는 실행하지 않았습니다. README 에 Chromium 환경용 실행 방법을 남겼고, 이번 라운드 스펙에서는 이 공백을 blocker 로 간주하지 않습니다.

## 남은 제한

- 저장 기능은 현재 브라우저의 로컬 저장소에만 보관되며, 브라우저 간 동기화나 내보내기/가져오기는 없습니다.
- 역할 선호도는 soft signal 로만 사용합니다. 티어 밸런스 우선순위는 유지하고, 같은 밸런스 버킷 안에서만 노출 후보 선택에 영향을 줍니다.
- 실제 브라우저 엔진 기준의 desktop/mobile 스모크는 Chromium 계열 브라우저가 있는 환경에서 후속 실행이 필요합니다.
- 이번 라운드 스펙에서는 이전 QA 에서 남아 있던 브라우저/모바일 실기기 검증 공백을 blocker 로 취급하지 않았고, 문서에도 그 상태를 그대로 남겼습니다.
