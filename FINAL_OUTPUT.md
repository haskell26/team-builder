# Final Output

## 이번 라운드에서 구현한 내용

- `matchPlayers` 를 현재 매치의 기준 상태로 두고, 붙여넣기 입력과 저장된 플레이어 불러오기가 같은 흐름으로 합쳐지도록 정리했습니다.
- 플레이어별 역할 선호도 `1순위 / 2순위 / 3순위` 를 현재 매치 UI 에서 직접 수정할 수 있게 했습니다.
- 역할 선호도가 비어 있으면 티어가 높은 역할 우선, 동점이면 `탱커 -> 딜러 -> 힐러` 순서로 기본 선호도를 자동 생성합니다.
- 후보 카드와 최종 편집 슬롯에 역할 선호도 배지(`1순위 / 2순위 / 3순위`)를 추가했고, 후보 재선택과 슬롯 스왑 뒤에도 즉시 다시 계산되도록 했습니다.
- 로컬 저장소 기반 플레이어 라이브러리를 추가했습니다.
  - `현재 10명 저장/업데이트` 로 현재 매치의 티어/선호도를 저장하거나 덮어씁니다.
  - 저장된 플레이어 목록은 앱 시작 시 자동으로 다시 불러옵니다.
  - 체크한 플레이어가 정확히 10명일 때만 `선택한 10명 불러오기` 가 활성화됩니다.
  - 저장된 10명을 불러오면 텍스트 입력창이 헤더 포함 canonical TSV 로 다시 채워지고, 후보/편집 결과는 비워진 상태로 돌아갑니다.
- 기존 기능도 유지했습니다.
  - 헤더 포함/미포함 입력 지원
  - 후보 6개 압축 미리보기
  - `1탱 2딜 2힐` 고정 슬롯 구조
  - 후보 선택 후 최종 편집 로드
  - 슬롯 두 번 클릭 스왑
  - 메인 UI 의 숫자 점수 비노출

## 사용 방법

1. 표를 붙여넣거나 저장된 플레이어 10명을 불러옵니다.
2. 현재 매치 영역에서 역할 선호도를 필요하면 수정합니다.
3. `팀 나누기` 를 눌러 후보 6개를 비교합니다.
4. 후보 카드를 눌러 최종 편집 영역으로 가져옵니다.
5. 슬롯 두 개를 차례대로 클릭해 즉시 스왑합니다.

## 실행 및 검증

- 실행: `npm test`
  - 결과: 통과
- 실행: `npm run build`
  - 결과: 통과, `dist/` 정적 번들 생성 확인
- 실행: `for name in chromium chromium-browser google-chrome google-chrome-stable microsoft-edge microsoft-edge-stable; do printf '%s: ' \"$name\"; command -v \"$name\" || true; done`
  - 결과: Chromium 계열 브라우저를 찾지 못했습니다.
- `npm run test:browser`
  - 실행하지 않음. 현재 환경에 Chromium 계열 브라우저가 없어 optional 브라우저 스모크를 수행할 수 없었습니다.

## 남은 제한

- 저장 기능은 현재 브라우저의 로컬 저장소에만 보관되며, 브라우저 간 동기화나 내보내기/가져오기는 없습니다.
- 실제 브라우저 엔진 기준의 desktop/mobile 스모크는 Chromium 계열 브라우저가 있는 환경에서 후속 실행이 필요합니다.
- 이번 스펙 기준으로는 기존 브라우저/모바일 실기기 검증 공백을 blocker 로 취급하지 않았습니다.
