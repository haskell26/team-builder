# Final Output

## 이번 라운드에서 구현한 내용

- 현재 10명 확인 영역을 큰 카드 스택 대신 한 줄 중심의 조밀한 요약 행으로 재구성했습니다.
- 각 현재 매치 행은 `이름 + 선호 상태 + 탱/딜/힐 티어 토큰 + 선호 6점 스텝 조정`을 한눈에 보여 주며, 스텝 버튼 id 패턴은 기존과 동일하게 유지했습니다.
- 현재 10명 영역은 별도 스크롤 컨테이너를 사용해 세로 길이가 과하게 늘어나지 않도록 정리했습니다.
- 추천 결과 상단에 `후보 6개 다시 생성` 버튼을 추가했습니다. 현재 붙여넣기 입력, 현재 10명, 저장된 플레이어 목록, 저장된 선택 상태를 유지한 채 후보 6개와 최종 편집 영역만 새로 계산합니다.
- reroll 후에는 새로 계산된 후보 1번이 편집 영역에 다시 로드되고, 이전 수동 스왑 상태는 버려집니다.
- 메인 UI 상단에 문의/건의사항을 `https://github.com/haskell26/team-builder/issues` 로 올려 달라는 실제 사용자용 안내 링크를 추가했습니다.
- 저장된 플레이어 불러오기 영역은 입력 오른쪽 사이드바, 기본 접힘, 한 줄 선택 hit area 구조를 그대로 유지했습니다.
- 저장된 플레이어 목록은 이름 중심 한 줄 리스트, 개별 삭제, 전체 삭제, exact-10 불러오기 흐름을 계속 지원합니다.
- 새로 붙여넣은 플레이어는 기본 `2 / 2 / 2` 로 시작하고, 저장된 예전 `preferenceOrder` 데이터는 `3 / 2 / 1` 점수 분배로 마이그레이션합니다.
- 현재 매치의 각 역할 점수는 스텝 버튼으로 직접 조정할 수 있고, 한 역할을 바꾸면 나머지 두 역할이 자동 조정되어 합계 6이 항상 유지됩니다.
- 후보 정렬은 여전히 `총점 차이 -> 역할군별 차이 합 -> 탱커 차이 -> 언랭 분배 차이 -> 완전 동률 버킷 랜덤` 순서를 유지합니다.
- 기존 핵심 기능도 유지했습니다: 헤더 포함/미포함 입력, 후보 6개 compact preview, `1탱 2딜 2힐` 고정 슬롯 구조, 후보 선택 후 최종 편집 로드, 슬롯 두 번 클릭 스왑, 메인 UI 의 숫자 밸런스 점수 비노출.

## 사용 방법

1. 표를 붙여넣거나, 오른쪽 `저장된 플레이어 불러오기` 보관함을 펼쳐 저장된 플레이어 10명을 선택해 불러옵니다.
2. 현재 매치 한 줄 요약 영역에서 각 플레이어의 `탱커 / 딜러 / 힐러` 점수를 조정합니다.
3. `팀 나누기` 를 눌러 후보 6개를 비교하고, 필요하면 `후보 6개 다시 생성`으로 같은 입력 상태에서 새 후보 묶음을 다시 뽑습니다.
4. 후보 카드를 눌러 최종 편집 영역으로 가져옵니다.
5. 슬롯 두 개를 차례대로 클릭해 즉시 스왑합니다.

문의나 건의사항은 메인 화면 링크 또는 `https://github.com/haskell26/team-builder/issues` 로 받습니다.

## 실행 및 검증

- 실행: `npm test`
  - 결과: 통과
- 실행: `npm run build`
  - 결과: 통과, `dist/` 정적 번들 생성 확인
- 참고: `npm run test:browser`
  - 이번 라운드에서는 실행하지 않았습니다. README 에 Chromium 환경용 실행 방법을 남겼고, 이번 라운드 스펙에서는 이 공백을 blocker 로 간주하지 않습니다.

## 남은 제한

- 저장 기능은 현재 브라우저의 로컬 저장소에만 보관되며, 브라우저 간 동기화나 내보내기/가져오기는 없습니다.
- 역할 선호도는 soft signal 로만 사용합니다. 티어 밸런스 우선순위는 유지하고, 같은 밸런스 버킷 안에서만 노출 후보 선택에 영향을 줍니다.
- 실제 브라우저 엔진 기준의 desktop/mobile 스모크는 Chromium 계열 브라우저가 있는 환경에서 후속 실행이 필요합니다.
- 이번 라운드 스펙에서는 이전 QA 에서 남아 있던 브라우저/모바일 실기기 검증 공백을 blocker 로 취급하지 않았고, 문서에도 그 상태를 그대로 남겼습니다.
